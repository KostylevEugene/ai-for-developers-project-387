# Шаблон переменных окружения (см. backend/.env.template).
# Для локального запуска uvicorn БД доступна на localhost:7235.
# По умолчанию (без DATABASE_URL) используется SQLite в ./data/zapisi.db.
PORT=8080
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:7235/zapisi
ADMIN_PASSWORD=admin