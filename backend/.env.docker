# Переменные окружения для запуска через docker-compose.
# В контейнере приложение обращается к службе БД "db" по внутреннему порту 5432.
PORT=8080
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/zapisi
ADMIN_PASSWORD=admin