# Шаблон переменных для локального запуска (uvicorn).
# БД подключена через docker-compose (сервис db), проброс порта 7235 -> 5432.
# Для запуска приложения в compose используйте .env.docker (URL на db:5432).
PORT=8080
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:7235/zapisi
ADMIN_PASSWORD=admin